action: create
event: ''
data:
  attrs:
    class: maximize
  type: case
  children:
  - type: text
    bind:
      value:
        default: chat ****
        source: login
    attrs:
      class: box shadow nogrow
      format: md
  - type: case
    attrs:
      horizontal: true
    children:
    - type: select
      attrs:
        class: --channel i jc box nogrow
      bind:
        value:
          target: channel
        options:
          source: channel
    - type: case
      attrs:
        class: f
      children:
      - type: rack
        attrs:
          class: --message box c as gap scrolly
          scroll: true
        bind:
          value:
            source: chat
        item:
        - type: case
          attrs:
            class: nogrow s ae accent box border shadow
            selector: ask
        - type: case
          attrs:
            class: nogrow s ax box border shadow
      - type: case
        attrs:
          class: --input nogrow box
          horizontal: true
        children:
        - type: case
          attrs:
            horizontal: true
          children:
          - type: input
            bind:
              value:
                target: message
