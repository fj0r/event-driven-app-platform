action: layout
attrs:
    class: maximize
type: container
children:
    - type: text
      value: chat with AI
      attrs:
          class: nogrow shadow
          format: md
      # data:
      #     event: test-data
    - type: list
      attrs:
          class: box xs gap scrolly
      data:
          event: chat
      item:
          - type: card
    - type: container
      attrs:
          class: nogrow box
          horizontal: true
      children:
        - type: container
          attrs:
            horizontal: true
          children:
            - type: input
              data:
                  upload: true
                  event: message
